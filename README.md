# RB-Extension
RB Extension allows users to quickly retrieve structured data using a predefined alias name — making data lookup fast, simple, and efficient.  Instead of searching manually or navigating complex interfaces, users can enter an alias and instantly fetch the associated information directly inside the browser.
